{% extends 'base.html.twig' %}

{% block homeLi %}
    <li class="nav-item active">
{% endblock %}

{% block main %}
    {% if error %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}
    <div class="container mgt">
        <div class="col-lg-12">
            <div class="alert alert-info">
                Przeczytaj o: <a target="_blank" href="{{ link.reference }}">{{ link.title }}</a>
            </div>
        </div>
    </div>
    <div class="container mgt">
        <div class="col-lg-4">
            Podstawy
            <a href="{{ absolute_url(path('basics', { "language": "JavaScript", "page": 1})) }}">JavaScriptu</a>
        </div>
        <div class="col-lg-4">
            Zadania z
            <a href="{{ absolute_url(path('tasks', { "language": "JavaScript", "page": 1})) }}">JavaScriptu</a>
        </div>
        <div class="col-lg-4">
            <a href="{{ absolute_url(path('courses')) }}">Kursy</a>
        </div>
    </div>
{% endblock %}
